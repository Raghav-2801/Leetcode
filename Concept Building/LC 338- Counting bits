class Solution:
    def countBits(self, n: int) -> List[int]:
        dp = [0] * (n + 1)
        for i in range(1, n + 1):
            dp[i] = dp[i & (i-1)] + 1

        return dp
        # def popcount(x):
        #     count = 0
        #     while x:
        #         x = x & (x - 1)
        #         count += 1

        #     return count

        # return [popcount(i) for i in range (n+1)]
